#include <iostream>
#include <vector>

using namespace std;

void PhanTich(vector<int> ans, int n, int sum) {
	for (int i = n; i > 0; --i) {
		
	}
}

main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	int t;
	cin >> t;
	while (t--) {
		int n;
		cin >> n;
		PhanTich({}, n);
	}
	return 0;
}
